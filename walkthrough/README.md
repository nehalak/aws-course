# Walkthroughs

Step-by-step solutions mirroring `aws-course/`. Each walkthrough shows:
- Exact commands (with expected output)
- Full CDK code
- What the Console should look like at each step
- Common errors + fixes

**Use walkthroughs only after attempting the exercises yourself.** The point is to learn; walkthroughs are reference + rescue.

Structure matches `aws-course/` 1:1.
